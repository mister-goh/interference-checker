import { describe, it, expect } from 'vitest'

// Scaffold smoke test — replaced by engine tests in task #4
describe('scaffold', () => {
  it('test runner is operational', () => {
    expect(1 + 1).toBe(2)
  })
})
